'------------------------------------------------------------------------------
' <autogenerated>
'     This code was generated Imports CSLA 3.7.X CodeSmith Templates.
'     Changes to this file will be lost after each regeneration.
'     To extend the functionality of this class, please modify the partial class 'Product.vb.
'
'     Template: SwitchableObject.DataAccess.cst
'     Template website: http://code.google.com/p/codesmith/
' </autogenerated>
'------------------------------------------------------------------------------
Option Explicit On
Option Strict On

Imports System

Imports Csla
Imports Csla.Data

Imports PetShop.Data

Public Partial Class Product
	
    #Region "Root Data Access"
	
	<RunLocal()> _
	Protected Overrides Sub DataPortal_Create()
		'MyBase.DataPortal_Create()

		ValidationRules.CheckRules()
	End Sub
	
    <Transactional(TransactionalTypes.TransactionScope)> _
	Private Shadows Sub DataPortal_Fetch(ByVal criteria As ProductCriteria)
	    Using reader As SafeDataReader = DataAccessLayer.Instance.ProductFetch(criteria.StateBag)
            If reader.Read() Then
	            Fetch(reader)
            End If
        End Using
    End Sub
	
	<Transactional(TransactionalTypes.TransactionScope)> _
	Protected Overrides Sub DataPortal_Insert()
		Using reader As SafeDataReader = DataAccessLayer.Instance.ProductInsert(ReadProperty(_productIdProperty), ReadProperty(_nameProperty), ReadProperty(_descnProperty), ReadProperty(_imageProperty), ReadProperty(_categoryIdProperty))
			If reader.Read() Then

			End If
		End Using
        
        FieldManager.UpdateChildren(Me)
	End Sub
	
	<Transactional(TransactionalTypes.TransactionScope)> _
	Protected Overrides Sub DataPortal_Update()
        Using reader As SafeDataReader = DataAccessLayer.Instance.ProductUpdate(ReadProperty(_productIdProperty), ReadProperty(_nameProperty), ReadProperty(_descnProperty), ReadProperty(_imageProperty), ReadProperty(_categoryIdProperty))
        End Using
        
        FieldManager.UpdateChildren(Me)
	End Sub
	
	<Transactional(TransactionalTypes.TransactionScope)> _
	Protected Overrides Sub DataPortal_DeleteSelf()
        DataPortal_Delete(new ProductCriteria(ProductId))
    End Sub
	
	<Transactional(TransactionalTypes.TransactionScope)> _
	Protected Overrides Sub DataPortal_Delete(ByVal criteria As Object)
		Dim theCriteria As ProductCriteria = DirectCast(criteria, ProductCriteria)
        If Not theCriteria Is Nothing Then
			Using reader As SafeDataReader = DataAccessLayer.Instance.ProductDelete(theCriteria.StateBag)
			End Using
		End If
    End Sub


	#End Region
	
	#Region "Child Data Access"

	<RunLocal()> _
    Protected Overrides Sub Child_Create()
		' TODO: load default values
		' omit this override if you have no defaults to set
		'MyBase.Child_Create()
    End Sub
	
	Private Sub Child_Fetch(ByVal criteria As Object)
		Dim theCriteria As ProductCriteria = DirectCast(criteria, ProductCriteria)
        If Not theCriteria Is Nothing Then
			Using reader As SafeDataReader = DataAccessLayer.Instance.ProductFetch(theCriteria.StateBag)
				If reader.Read() Then
					Fetch(reader)
				End If
			End Using
		End If
        
        MarkAsChild()
	End Sub
	
	Private Sub Child_Insert()
	    Using reader As SafeDataReader = DataAccessLayer.Instance.ProductInsert(ReadProperty(_productIdProperty), ReadProperty(_nameProperty), ReadProperty(_descnProperty), ReadProperty(_imageProperty), ReadProperty(_categoryIdProperty))
			If reader.Read() Then

			End If
		End Using
	End Sub
	
	Private Sub Child_Update()
        Using reader As SafeDataReader = DataAccessLayer.Instance.ProductUpdate(ReadProperty(_productIdProperty), ReadProperty(_nameProperty), ReadProperty(_descnProperty), ReadProperty(_imageProperty), ReadProperty(_categoryIdProperty))
        End Using
	End Sub
	
	Private Sub Child_DeleteSelf()
        DataPortal_Delete(new ProductCriteria(ProductId))
    End Sub

	#End Region

    Private Sub Fetch(ByVal reader As SafeDataReader)
		LoadProperty(_productIdProperty, reader.GetString("ProductId"))
		LoadProperty(_nameProperty, reader.GetString("Name"))
		LoadProperty(_descnProperty, reader.GetString("Descn"))
		LoadProperty(_imageProperty, reader.GetString("Image"))

		LoadProperty(_categoryIdProperty, reader.GetString("CategoryId"))

        MarkOld()
    End Sub
    
End Class