'------------------------------------------------------------------------------
' <autogenerated>
'     This code was generated Imports CSLA 3.7.X CodeSmith Templates.
'     Changes to this file will be lost after each regeneration.
'     To extend the functionality of this class, please modify the partial class 'Profile.vb.
'
'     Template: SwitchableObject.DataAccess.cst
'     Template website: http://code.google.com/p/codesmith/
' </autogenerated>
'------------------------------------------------------------------------------
Option Explicit On
Option Strict On

Imports System

Imports Csla
Imports Csla.Data

Imports PetShop.Data

Public Partial Class Profile
	
    #Region "Root Data Access"
	
	<RunLocal()> _
	Protected Overrides Sub DataPortal_Create()
		'MyBase.DataPortal_Create()

		ValidationRules.CheckRules()
	End Sub
	
    <Transactional(TransactionalTypes.TransactionScope)> _
	Private Shadows Sub DataPortal_Fetch(ByVal criteria As ProfileCriteria)
	    Using reader As SafeDataReader = DataAccessLayer.Instance.ProfileFetch(criteria.StateBag)
            If reader.Read() Then
	            Fetch(reader)
            End If
        End Using
    End Sub
	
	<Transactional(TransactionalTypes.TransactionScope)> _
	Protected Overrides Sub DataPortal_Insert()
		Using reader As SafeDataReader = DataAccessLayer.Instance.ProfileInsert(ReadProperty(_usernameProperty), ReadProperty(_applicationNameProperty), ReadProperty(_isAnonymousProperty), ReadProperty(_lastActivityDateProperty), ReadProperty(_lastUpdatedDateProperty))
			If reader.Read() Then

				LoadProperty(_uniqueIDProperty, reader.GetInt32("UniqueID"))
			End If
		End Using
        
        FieldManager.UpdateChildren(Me)
	End Sub
	
	<Transactional(TransactionalTypes.TransactionScope)> _
	Protected Overrides Sub DataPortal_Update()
        Using reader As SafeDataReader = DataAccessLayer.Instance.ProfileUpdate(ReadProperty(_uniqueIDProperty), ReadProperty(_usernameProperty), ReadProperty(_applicationNameProperty), ReadProperty(_isAnonymousProperty), ReadProperty(_lastActivityDateProperty), ReadProperty(_lastUpdatedDateProperty))
        End Using
        
        FieldManager.UpdateChildren(Me)
	End Sub
	
	<Transactional(TransactionalTypes.TransactionScope)> _
	Protected Overrides Sub DataPortal_DeleteSelf()
        DataPortal_Delete(new ProfileCriteria(UniqueID))
    End Sub
	
	<Transactional(TransactionalTypes.TransactionScope)> _
	Protected Overrides Sub DataPortal_Delete(ByVal criteria As Object)
		Dim theCriteria As ProfileCriteria = DirectCast(criteria, ProfileCriteria)
        If Not theCriteria Is Nothing Then
			Using reader As SafeDataReader = DataAccessLayer.Instance.ProfileDelete(theCriteria.StateBag)
			End Using
		End If
    End Sub


	#End Region
	
	#Region "Child Data Access"

	<RunLocal()> _
    Protected Overrides Sub Child_Create()
		' TODO: load default values
		' omit this override if you have no defaults to set
		'MyBase.Child_Create()
    End Sub
	
	Private Sub Child_Fetch(ByVal reader As SafeDataReader)
		Fetch(reader)
        
        MarkAsChild()
	End Sub
	
	Private Sub Child_Insert()
	    Using reader As SafeDataReader = DataAccessLayer.Instance.ProfileInsert(ReadProperty(_usernameProperty), ReadProperty(_applicationNameProperty), ReadProperty(_isAnonymousProperty), ReadProperty(_lastActivityDateProperty), ReadProperty(_lastUpdatedDateProperty))
			If reader.Read() Then

				LoadProperty(_uniqueIDProperty, reader.GetInt32("UniqueID"))
			End If
		End Using
	End Sub
	
	Private Sub Child_Update()
        Using reader As SafeDataReader = DataAccessLayer.Instance.ProfileUpdate(ReadProperty(_uniqueIDProperty), ReadProperty(_usernameProperty), ReadProperty(_applicationNameProperty), ReadProperty(_isAnonymousProperty), ReadProperty(_lastActivityDateProperty), ReadProperty(_lastUpdatedDateProperty))
        End Using
	End Sub
	
	Private Sub Child_DeleteSelf()
        DataPortal_Delete(new ProfileCriteria(UniqueID))
    End Sub

	#End Region

    Private Sub Fetch(ByVal reader As SafeDataReader)
		LoadProperty(_uniqueIDProperty, reader.GetInt32("UniqueID"))
		LoadProperty(_usernameProperty, reader.GetString("Username"))
		LoadProperty(_applicationNameProperty, reader.GetString("ApplicationName"))
		LoadProperty(_isAnonymousProperty, reader.GetBoolean("IsAnonymous"))
		LoadProperty(_lastActivityDateProperty, reader.GetDateTime("LastActivityDate"))
		LoadProperty(_lastUpdatedDateProperty, reader.GetDateTime("LastUpdatedDate"))


        MarkOld()
    End Sub
    
End Class