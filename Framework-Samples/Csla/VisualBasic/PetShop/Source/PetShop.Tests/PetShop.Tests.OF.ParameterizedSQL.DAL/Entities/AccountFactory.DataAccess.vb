'------------------------------------------------------------------------------
' <autogenerated>
'     This code was generated using CodeSmith: v6.0.2, CSLA Templates: v3.0.3.2430, CSLA Framework: v4.0.0.
'     Changes to this file will be lost after each regeneration.
'     To extend the functionality of this class, please modify the partial class 'Account.cs'.
'
'     Template: ObjectFactory.DataAccess.ParameterizedSQL.cst
'     Template website: http://code.google.com/p/codesmith/
' </autogenerated>
'------------------------------------------------------------------------------
Option Explicit On
Option Strict On

#Region "Imports declarations"

Imports System
Imports System.Data
Imports System.Data.SqlClient

Imports Csla
Imports Csla.Data
Imports Csla.Server

Imports PetShop.Tests.OF.ParameterizedSQL

#End Region

Namespace PetShop.Tests.OF.ParameterizedSQL.DAL
    Public Partial Class AccountFactory
        Inherits ObjectFactory
    
#Region "Create"
    
        ''' <summary>
        ''' Creates New Account with default values.
        ''' </summary>
        ''' <Returns>New Account.</Returns>
        <RunLocal()> _
        Public Function Create() As Account
            Dim item As Account = CType(Activator.CreateInstance(GetType(Account), True), Account)
    
            Dim cancel As Boolean = False
            OnCreating(cancel)
            If (cancel) Then
                Return item
            End If
    
            Using BypassPropertyChecks(item)
                ' Default values.
    
    
                CheckRules(item)
                MarkNew(item)
                MarkAsChild(item)
            End Using
    
            OnCreated()
    
            Return item
        End Function
    
        ''' <summary>
        ''' Creates New Account with default values.
        ''' </summary>
        ''' <Returns>New Account.</Returns>
        <RunLocal()> _
        Private Function Create(ByVal criteria As AccountCriteria) As  Account
            Dim item As Account = CType(Activator.CreateInstance(GetType(Account), True), Account)
    
            Dim cancel As Boolean = False
            OnCreating(cancel)
            If (cancel) Then
                Return item
            End If
    
            Dim resource As Account = Fetch(criteria)
    
            Using BypassPropertyChecks(item)
                item.Email = resource.Email
                item.FirstName = resource.FirstName
                item.LastName = resource.LastName
                item.Address1 = resource.Address1
                item.Address2 = resource.Address2
                item.City = resource.City
                item.State = resource.State
                item.Zip = resource.Zip
                item.Country = resource.Country
                item.Phone = resource.Phone
            End Using
    
            CheckRules(item)
            MarkNew(item)
            MarkAsChild(item)
    
            OnCreated()
    
            Return item
        End Function
    
#End Region
    
#Region "Fetch
    
        ''' <summary>
        ''' Fetch Account.
        ''' </summary>
        ''' <param name="criteria">The criteria.</param>
        ''' <Returns></Returns>
        Public Function Fetch(ByVal criteria As AccountCriteria) As Account
            Dim item As Account = Nothing
            
            Dim cancel As Boolean = False
            OnFetching(criteria, cancel)
            If (cancel) Then
                Return item
            End If
    
            Dim commandText As String = String.Format("SELECT [AccountId], [UniqueID], [Email], [FirstName], [LastName], [Address1], [Address2], [City], [State], [Zip], [Country], [Phone] FROM [dbo].[Account] {0}", ADOHelper.BuildWhereStatement(criteria.StateBag))
            Using connection As New SqlConnection(ADOHelper.ConnectionString)
                connection.Open()
                Using command As New SqlCommand(commandText, connection)
                    command.Parameters.AddRange(ADOHelper.SqlParameters(criteria.StateBag))
                    Using reader As SafeDataReader = New SafeDataReader(command.ExecuteReader())
                        If reader.Read() Then
                            item = Map(reader)
                        Else
                            Throw New Exception(String.Format("The record was not found in 'Account' using the following criteria: {0}.", criteria))
                        End If
                    End Using
                End Using
            End Using
    
            MarkOld(item)
            MarkAsChild(item)
    
            OnFetched()
    
            Return item
        End Function
    
#End Region
    
#Region "Insert"
    
        Private Sub DoInsert(ByRef item As Account, ByVal stopProccessingChildren As Boolean)
            ' Don't update If the item isn't dirty.
            If Not (item.IsDirty) Then
                Return
            End If
    
            Dim cancel As Boolean = False
            OnInserting(cancel)
            If (cancel) Then
                Return
            End If
    
            Const commandText As String = "INSERT INTO [dbo].[Account] ([UniqueID], [Email], [FirstName], [LastName], [Address1], [Address2], [City], [State], [Zip], [Country], [Phone]) VALUES (@p_UniqueID, @p_Email, @p_FirstName, @p_LastName, @p_Address1, @p_Address2, @p_City, @p_State, @p_Zip, @p_Country, @p_Phone); SELECT [AccountId] FROM [dbo].[Account] WHERE AccountId = SCOPE_IDENTITY()"
            Using connection As New SqlConnection(ADOHelper.ConnectionString)
                connection.Open()
                Using command As New SqlCommand(commandText, connection)
                    command.Parameters.AddWithValue("@p_UniqueID", item.UniqueID)
				command.Parameters.AddWithValue("@p_Email", item.Email)
				command.Parameters.AddWithValue("@p_FirstName", item.FirstName)
				command.Parameters.AddWithValue("@p_LastName", item.LastName)
				command.Parameters.AddWithValue("@p_Address1", item.Address1)
				command.Parameters.AddWithValue("@p_Address2", ADOHelper.NullCheck(item.Address2))
				command.Parameters.AddWithValue("@p_City", item.City)
				command.Parameters.AddWithValue("@p_State", item.State)
				command.Parameters.AddWithValue("@p_Zip", item.Zip)
				command.Parameters.AddWithValue("@p_Country", item.Country)
				command.Parameters.AddWithValue("@p_Phone", ADOHelper.NullCheck(item.Phone))
    
                    Using reader As SafeDataReader = New SafeDataReader(command.ExecuteReader())
                        If reader.Read() Then
    
                            item.AccountId = reader.GetInt32("AccountId")
                        End If
                    End Using
                End Using
            End Using
    
    
            MarkOld(item)
            CheckRules(item)
            
            If Not (stopProccessingChildren) Then
                ' Update Child Items.
    
    
                Update_Profile_ProfileMember_UniqueID(item)
    
            End If
    
            OnInserted()
        End Sub
    
#End Region
    
#Region "Update"
    
        <Transactional(TransactionalTypes.TransactionScope)> _
        Public Function Update(ByVal item As Account) As Account
            Return Update(item, false)
        End Function
    
        Public Function Update(ByVal item As Account, ByVal stopProccessingChildren as Boolean) As Account
            If(item.IsDeleted) Then
                DoDelete(item)
                MarkNew(item)
            Else If(item.IsNew) Then
                DoInsert(item, stopProccessingChildren)
            Else
            DoUpdate(item, stopProccessingChildren)
            End If
    
            Return item
        End Function
    
        Private Sub DoUpdate(ByRef item As Account, ByVal stopProccessingChildren As Boolean)
            Dim cancel As Boolean = False
            OnUpdating(cancel)
            If (cancel) Then
                Return
            End If
            
            ' Don't update If the item isn't dirty.
            If (item.IsDirty) Then
    
                Const commandText As String = "UPDATE [dbo].[Account]  SET [UniqueID] = @p_UniqueID, [Email] = @p_Email, [FirstName] = @p_FirstName, [LastName] = @p_LastName, [Address1] = @p_Address1, [Address2] = @p_Address2, [City] = @p_City, [State] = @p_State, [Zip] = @p_Zip, [Country] = @p_Country, [Phone] = @p_Phone WHERE [AccountId] = @p_AccountId"
                Using connection As New SqlConnection(ADOHelper.ConnectionString)
                    connection.Open()
                    Using command As New SqlCommand(commandText, connection)
                        command.Parameters.AddWithValue("@p_AccountId", item.AccountId)
				command.Parameters.AddWithValue("@p_UniqueID", item.UniqueID)
				command.Parameters.AddWithValue("@p_Email", item.Email)
				command.Parameters.AddWithValue("@p_FirstName", item.FirstName)
				command.Parameters.AddWithValue("@p_LastName", item.LastName)
				command.Parameters.AddWithValue("@p_Address1", item.Address1)
				command.Parameters.AddWithValue("@p_Address2", ADOHelper.NullCheck(item.Address2))
				command.Parameters.AddWithValue("@p_City", item.City)
				command.Parameters.AddWithValue("@p_State", item.State)
				command.Parameters.AddWithValue("@p_Zip", item.Zip)
				command.Parameters.AddWithValue("@p_Country", item.Country)
				command.Parameters.AddWithValue("@p_Phone", ADOHelper.NullCheck(item.Phone))
    
                        Using reader As SafeDataReader = New SafeDataReader(command.ExecuteReader())
                            'RecordsAffected: The number of rows changed, inserted, or deleted. -1 for select statements; 0 if no rows were affected, or the statement failed. 
                            If reader.RecordsAffected = 0 Then
                                Throw New DBConcurrencyException("The entity is out of date on the client. Please update the entity and try again. This could also be thrown if the sql statement failed to execute.")
                            End If
        
                            If reader.Read() Then
                                item.AccountId = reader.GetInt32("AccountId")
                            End If
                        End Using
                    End Using
                End Using
            End If
    
    
            MarkOld(item)
            CheckRules(item)
    
            If Not (stopProccessingChildren) Then
                ' Update Child Items.
    
    
                Update_Profile_ProfileMember_UniqueID(item)
    
            End If
    
            OnUpdated()
        End Sub
    
#End Region
    
#Region "Delete"
    
        <Transactional(TransactionalTypes.TransactionScope)> _
        Public Sub Delete(ByVal criteria As AccountCriteria)
            ' Note: this call to delete is for immediate deletion and doesn't keep track of any entity state.
            DoDelete(criteria)
        End Sub
    
        Protected Sub DoDelete(ByRef item As Account)
            ' If we're not dirty then don't update the database.
            If Not (item.IsDirty) Then
                Return
            End If
    
            ' If we're New then don't call delete.
            If (item.IsNew) Then
                Return
            End If
    
            Dim criteria As New AccountCriteria()
    criteria.AccountId = item.AccountId
            DoDelete(criteria)
    
            MarkNew(item)
        End Sub
    
        ''' <summary>
        ''' This call to delete is for immediate deletion and doesn't keep track of any entity state.
        ''' </summary>
        ''' <param name="criteria">The Criteria.</param>
        Private Sub DoDelete(ByVal criteria As AccountCriteria)
            Dim cancel As Boolean = False
            OnDeleting(criteria, cancel)
            If (cancel) Then
                Return
            End If
    
            Dim commandText As String = String.Format("DELETE FROM [dbo].[Account] {0}", ADOHelper.BuildWhereStatement(criteria.StateBag))
            Using connection As New SqlConnection(ADOHelper.ConnectionString)
                connection.Open()
                Using command As New SqlCommand(commandText, connection)
                    command.Parameters.AddRange(ADOHelper.SqlParameters(criteria.StateBag))
    
                    'result: The number of rows changed, inserted, or deleted. -1 for select statements; 0 if no rows were affected, or the statement failed. 
                    Dim result As Integer = command.ExecuteNonQuery()
                    If (result = 0) Then
                        throw new DBConcurrencyException("The entity is out of date on the client. Please update the entity and try again. This could also be thrown if the sql statement failed to execute.")
                    End If
                End Using
            End Using
    
            OnDeleted()
        End Sub
    
#End Region
    
#Region "Helper Methods"
    
        Public Function Map(ByVal reader As SafeDataReader) As Account
            Dim item As Account = CType(Activator.CreateInstance(GetType(Account), True), Account)
            Using BypassPropertyChecks(item)
                item.AccountId = reader.GetInt32("AccountId")
                item.UniqueID = reader.GetInt32("UniqueID")
                item.Email = reader.GetString("Email")
                item.FirstName = reader.GetString("FirstName")
                item.LastName = reader.GetString("LastName")
                item.Address1 = reader.GetString("Address1")
                item.Address2 = reader.GetString("Address2")
                item.City = reader.GetString("City")
                item.State = reader.GetString("State")
                item.Zip = reader.GetString("Zip")
                item.Country = reader.GetString("Country")
                item.Phone = reader.GetString("Phone")
            End Using
            MarkOld(item)
            MarkAsChild(item)
    
            Return item
        End Function
    
    
    
        'AssociatedManyToOne
        Private Shared Sub  Update_Profile_ProfileMember_UniqueID(ByRef item As Account)
    		item.ProfileMember.UniqueID = item.UniqueID
    
            Dim factory As New ProfileFactory
            factory.Update(item.ProfileMember, True)
        End Sub
    
    
#End Region
    
#Region "DataPortal partial methods"
    
        Partial Private Sub OnCreating(ByRef cancel As Boolean)
        End Sub
        Partial Private Sub OnCreated()
        End Sub
        Partial Private Sub OnFetching(ByVal criteria As AccountCriteria, ByRef cancel As Boolean)
        End Sub
        Partial Private Sub OnFetched()
        End Sub
        Partial Private Sub OnMapping(ByVal reader As SafeDataReader, ByRef cancel As Boolean)
        End Sub
        Partial Private Sub OnMapped()
        End Sub
        Partial Private Sub OnInserting(ByRef cancel As Boolean)
        End Sub
        Partial Private Sub OnInserted()
        End Sub
        Partial Private Sub OnUpdating(ByRef cancel As Boolean)
        End Sub
        Partial Private Sub OnUpdated()
        End Sub
        Partial Private Sub OnSelfDeleting(ByRef cancel As Boolean)
        End Sub
        Partial Private Sub OnSelfDeleted()
        End Sub
        Partial Private Sub OnDeleting(ByVal criteria As AccountCriteria, ByRef cancel As Boolean)
        End Sub
        Partial Private Sub OnDeleted()
        End Sub
        Private Partial Sub OnChildLoading(ByVal childProperty As Csla.Core.IPropertyInfo, ByRef cancel As Boolean)
        End Sub
    
#End Region
    End Class
End Namespace