'------------------------------------------------------------------------------
' <autogenerated>
'     This code was generated using CodeSmith: v6.0.2, CSLA Templates: v3.0.3.2430, CSLA Framework: v4.0.0.
'     Changes to this file will be lost after each regeneration.
'     To extend the functionality of this class, please modify the partial class 'Profile.cs'.
'
'     Template: ObjectFactory.DataAccess.StoredProcedures.cst
'     Template website: http://code.google.com/p/codesmith/
' </autogenerated>
'------------------------------------------------------------------------------
Option Explicit On
Option Strict On

#Region "Imports declarations"

Imports System
Imports System.Data
Imports System.Data.SqlClient

Imports Csla
Imports Csla.Data
Imports Csla.Server

Imports PetShop.Tests.OF.StoredProcedures

#End Region

Namespace PetShop.Tests.OF.StoredProcedures.DAL
    Public Partial Class ProfileFactory
        Inherits ObjectFactory
    
#Region "Create"
    
        ''' <summary>
        ''' Creates New Profile with default values.
        ''' </summary>
        ''' <Returns>New Profile.</Returns>
        <RunLocal()> _
        Public Function Create() As Profile
            Dim item As Profile = CType(Activator.CreateInstance(GetType(Profile), True), Profile)
    
            Dim cancel As Boolean = False
            OnCreating(cancel)
            If (cancel) Then
                Return item
            End If
    
            Using BypassPropertyChecks(item)
                ' Default values.
    
    
                CheckRules(item)
                MarkNew(item)
            End Using
    
            OnCreated()
    
            Return item
        End Function
    
        ''' <summary>
        ''' Creates New Profile with default values.
        ''' </summary>
        ''' <Returns>New Profile.</Returns>
        <RunLocal()> _
        Private Function Create(ByVal criteria As ProfileCriteria) As  Profile
            Dim item As Profile = CType(Activator.CreateInstance(GetType(Profile), True), Profile)
    
            Dim cancel As Boolean = False
            OnCreating(cancel)
            If (cancel) Then
                Return item
            End If
    
            Dim resource As Profile = Fetch(criteria)
    
            Using BypassPropertyChecks(item)
                item.Username = resource.Username
                item.ApplicationName = resource.ApplicationName
                item.IsAnonymous = resource.IsAnonymous
                item.LastActivityDate = resource.LastActivityDate
                item.LastUpdatedDate = resource.LastUpdatedDate
            End Using
    
            CheckRules(item)
            MarkNew(item)
    
            OnCreated()
    
            Return item
        End Function
    
#End Region
    
#Region "Fetch"
    
        ''' <summary>
        ''' Fetch Profile.
        ''' </summary>
        ''' <param name="criteria">The criteria.</param>
        ''' <Returns></Returns>
        Public Function Fetch(ByVal criteria As ProfileCriteria) As Profile
            Dim item As Profile = Nothing
            
            Dim cancel As Boolean = False
            OnFetching(criteria, cancel)
            If (cancel) Then
                Return item
            End If
    
            Using connection As New SqlConnection(ADOHelper.ConnectionString)
                connection.Open()
                Using command As New SqlCommand("[dbo].[CSLA_Profile_Select]", connection)
                    command.CommandType = CommandType.StoredProcedure
                    command.Parameters.AddRange(ADOHelper.SqlParameters(criteria.StateBag))
                    command.Parameters.AddWithValue("@p_IsAnonymousHasValue", criteria.IsAnonymousHasValue)
					command.Parameters.AddWithValue("@p_LastActivityDateHasValue", criteria.LastActivityDateHasValue)
					command.Parameters.AddWithValue("@p_LastUpdatedDateHasValue", criteria.LastUpdatedDateHasValue)
                    Using reader As SafeDataReader = New SafeDataReader(command.ExecuteReader())
                        If reader.Read() Then
                            item = Map(reader)
                        Else
                            Throw New Exception(String.Format("The record was not found in 'Profiles' using the following criteria: {0}.", criteria))
                        End If
                    End Using
                End Using
            End Using
    
            MarkOld(item)
    
            OnFetched()
    
            Return item
        End Function
    
#End Region
    
#Region "Insert"
    
        Private Sub DoInsert(ByRef item As Profile, ByVal stopProccessingChildren As Boolean)
            ' Don't update If the item isn't dirty.
            If Not (item.IsDirty) Then
                Return
            End If
    
            Dim cancel As Boolean = False
            OnInserting(cancel)
            If (cancel) Then
                Return
            End If
    
            Using connection As New SqlConnection(ADOHelper.ConnectionString)
                connection.Open()
                Using command As New SqlCommand("[dbo].[CSLA_Profile_Insert]", connection)
                    command.CommandType = CommandType.StoredProcedure
                    command.Parameters.AddWithValue("@p_UniqueID", item.UniqueID)
				command.Parameters("@p_UniqueID").Direction = ParameterDirection.Output
				command.Parameters.AddWithValue("@p_Username", item.Username)
				command.Parameters.AddWithValue("@p_ApplicationName", item.ApplicationName)
				command.Parameters.AddWithValue("@p_IsAnonymous", ADOHelper.NullCheck(item.IsAnonymous))
				command.Parameters.AddWithValue("@p_LastActivityDate", ADOHelper.NullCheck(item.LastActivityDate))
				command.Parameters.AddWithValue("@p_LastUpdatedDate", ADOHelper.NullCheck(item.LastUpdatedDate))
    
                    command.ExecuteNonQuery()
    
                        item.UniqueID = DirectCast(command.Parameters("@p_UniqueID").Value, System.Int32)
                End Using
            End Using
    
    
            MarkOld(item)
            CheckRules(item)
            
            If Not (stopProccessingChildren) Then
                ' Update Child Items.
    
    
    
                Update_Account_Accounts_UniqueID(item)
                Update_Cart_Carts_UniqueID(item)
            End If
    
            OnInserted()
        End Sub
    
#End Region
    
#Region "Update"
    
        <Transactional(TransactionalTypes.TransactionScope)> _
        Public Function Update(ByVal item As Profile) As Profile
            Return Update(item, false)
        End Function
    
        Public Function Update(ByVal item As Profile, ByVal stopProccessingChildren as Boolean) As Profile
            If(item.IsDeleted) Then
                DoDelete(item)
                MarkNew(item)
            Else If(item.IsNew) Then
                DoInsert(item, stopProccessingChildren)
            Else
            DoUpdate(item, stopProccessingChildren)
            End If
    
            Return item
        End Function
    
        Private Sub DoUpdate(ByRef item As Profile, ByVal stopProccessingChildren As Boolean)
            Dim cancel As Boolean = False
            OnUpdating(cancel)
            If (cancel) Then
                Return
            End If
    
            ' Don't update If the item isn't dirty.
            If (item.IsDirty) Then
    
                Using connection As New SqlConnection(ADOHelper.ConnectionString)
                    connection.Open()
                    Using command As New SqlCommand("[dbo].[CSLA_Profile_Update]", connection)
                        command.CommandType = CommandType.StoredProcedure
                        command.Parameters.AddWithValue("@p_UniqueID", item.UniqueID)
				command.Parameters.AddWithValue("@p_Username", item.Username)
				command.Parameters.AddWithValue("@p_ApplicationName", item.ApplicationName)
				command.Parameters.AddWithValue("@p_IsAnonymous", ADOHelper.NullCheck(item.IsAnonymous))
				command.Parameters.AddWithValue("@p_LastActivityDate", ADOHelper.NullCheck(item.LastActivityDate))
				command.Parameters.AddWithValue("@p_LastUpdatedDate", ADOHelper.NullCheck(item.LastUpdatedDate))
    
                        'result: The number of rows changed, inserted, or deleted. -1 for select statements; 0 if no rows were affected, or the statement failed. 
                        Dim result As Integer = command.ExecuteNonQuery()
                        If (result = 0) Then
                            throw new DBConcurrencyException("The entity is out of date on the client. Please update the entity and try again. This could also be thrown if the sql statement failed to execute.")
                        End If
                        
                        item.UniqueID = DirectCast(command.Parameters("@p_UniqueID").Value, System.Int32)
                    End Using
                End Using
            End If
    
    
            MarkOld(item)
            CheckRules(item)
    
            If Not (stopProccessingChildren) Then
                ' Update Child Items.
    
    
    
                Update_Account_Accounts_UniqueID(item)
                Update_Cart_Carts_UniqueID(item)
            End If
    
            OnUpdated()
        End Sub
    
#End Region
    
#Region "Delete"
    
        <Transactional(TransactionalTypes.TransactionScope)> _
        Public Sub Delete(ByVal criteria As ProfileCriteria)
            ' Note: this call to delete is for immediate deletion and doesn't keep track of any entity state.
            DoDelete(criteria)
        End Sub
    
        Protected Sub DoDelete(ByRef item As Profile)
            ' If we're not dirty then don't update the database.
            If Not (item.IsDirty) Then
                Return
            End If
    
            ' If we're New then don't call delete.
            If (item.IsNew) Then
                Return
            End If
    
            Dim criteria As New ProfileCriteria()
    criteria.UniqueID = item.UniqueID
            DoDelete(criteria)
    
            MarkNew(item)
        End Sub
    
        Private Sub DoDelete(ByVal criteria As ProfileCriteria)
            Dim cancel As Boolean = False
            OnDeleting(criteria, cancel)
            If (cancel) Then
                Return
            End If
    
            Using connection As New SqlConnection(ADOHelper.ConnectionString)
                connection.Open()
                Using command As New SqlCommand("[dbo].[CSLA_Profile_Delete]", connection)
                    command.CommandType = CommandType.StoredProcedure
                    command.Parameters.AddRange(ADOHelper.SqlParameters(criteria.StateBag))
    
                    'result: The number of rows changed, inserted, or deleted. -1 for select statements; 0 if no rows were affected, or the statement failed. 
                    Dim result As Integer = command.ExecuteNonQuery()
                    If (result = 0) Then
                        throw new DBConcurrencyException("The entity is out of date on the client. Please update the entity and try again. This could also be thrown if the sql statement failed to execute.")
                    End If
                End Using
            End Using
    
            OnDeleted()
        End Sub
    
#End Region
    
#Region "Helper Methods"
    
        Public Function Map(ByVal reader As SafeDataReader) As Profile
            Dim item As Profile = CType(Activator.CreateInstance(GetType(Profile), True), Profile)
            Using BypassPropertyChecks(item)
                item.UniqueID = reader.GetInt32("UniqueID")
                item.Username = reader.GetString("Username")
                item.ApplicationName = reader.GetString("ApplicationName")
                item.IsAnonymous = If(reader.IsDBNull("IsAnonymous"), Nothing, reader.GetBoolean("IsAnonymous"))
                item.LastActivityDate = If(reader.IsDBNull("LastActivityDate"), Nothing, reader.GetDateTime("LastActivityDate"))
                item.LastUpdatedDate = If(reader.IsDBNull("LastUpdatedDate"), Nothing, reader.GetDateTime("LastUpdatedDate"))
            End Using
    
            MarkOld(item)

            Return item
        End Function
    
    
    
    
        'AssociatedOneToMany
        Private Shared Sub Update_Account_Accounts_UniqueID(ByRef item As Profile)
            For Each itemToUpdate As Account In item.Accounts
    		itemToUpdate.UniqueID = item.UniqueID
    
                Dim factory As New AccountFactory
                factory.Update(itemToUpdate, True)
            Next
        End Sub
        'AssociatedOneToMany
        Private Shared Sub Update_Cart_Carts_UniqueID(ByRef item As Profile)
            For Each itemToUpdate As Cart In item.Carts
    		itemToUpdate.UniqueID = item.UniqueID
    
                Dim factory As New CartFactory
                factory.Update(itemToUpdate, True)
            Next
        End Sub
    
#End Region
    
#Region "DataPortal partial methods"
    
        Partial Private Sub OnCreating(ByRef cancel As Boolean)
        End Sub
        Partial Private Sub OnCreated()
        End Sub
        Partial Private Sub OnFetching(ByVal criteria As ProfileCriteria, ByRef cancel As Boolean)
        End Sub
        Partial Private Sub OnFetched()
        End Sub
        Partial Private Sub OnMapping(ByVal reader As SafeDataReader, ByRef cancel As Boolean)
        End Sub
        Partial Private Sub OnMapped()
        End Sub
        Partial Private Sub OnInserting(ByRef cancel As Boolean)
        End Sub
        Partial Private Sub OnInserted()
        End Sub
        Partial Private Sub OnUpdating(ByRef cancel As Boolean)
        End Sub
        Partial Private Sub OnUpdated()
        End Sub
        Partial Private Sub OnSelfDeleting(ByRef cancel As Boolean)
        End Sub
        Partial Private Sub OnSelfDeleted()
        End Sub
        Partial Private Sub OnDeleting(ByVal criteria As ProfileCriteria, ByRef cancel As Boolean)
        End Sub
        Partial Private Sub OnDeleted()
        End Sub
        Private Partial Sub OnChildLoading(ByVal childProperty As Csla.Core.IPropertyInfo, ByRef cancel As Boolean)
        End Sub
    
#End Region
    End Class
End Namespace