
'------------------------------------------------------------------------------
' <autogenerated>
'     Me code was generated Imports CSLA 3.7.X CodeSmith Templates.
'     Changes to Me file will be lost after each regeneration.
'     To extend the functionality of Me class, please modify the partial class 'Inventory.vb.
'
'     Template: EditableRoot.DataAccess.cst
'     Template website: http://code.google.com/p/codesmith/
' </autogenerated>
'------------------------------------------------------------------------------
Option Explicit On
Option Strict On

Imports System

Imports Csla
Imports Csla.Data
Imports Csla.Validation

Imports PetShop.Data

Public Partial Class Inventory
		
	#Region "Data Access"

	<RunLocal()> _
	Protected Shadows Sub DataPortal_Create()
		'MyBase.DataPortal_Create()

		ValidationRules.CheckRules()
	End Sub

	Private Shadows Sub DataPortal_Fetch(ByVal criteria As InventoryCriteria)
	    Using reader As SafeDataReader = DataAccessLayer.Instance.InventoryFetch(criteria.StateBag)
            If reader.Read() Then
	            Fetch(reader)
            End If
        End Using
    End Sub
	
    Private Sub Fetch(ByVal reader As SafeDataReader)
		LoadProperty(_itemIdProperty, reader.GetString("ItemId"))
		LoadProperty(_qtyProperty, reader.GetInt32("Qty"))


        MarkOld()
    End Sub
	
	<Transactional(TransactionalTypes.TransactionScope)> _
	Protected Overrides Sub DataPortal_Insert()
		Using reader As SafeDataReader = DataAccessLayer.Instance.InventoryInsert(ReadProperty(_itemIdProperty), ReadProperty(_qtyProperty))
			If reader.Read() Then

			End If
		End Using
        
	End Sub
	
	<Transactional(TransactionalTypes.TransactionScope)> _
	Protected Overrides Sub DataPortal_Update()
        Using reader As SafeDataReader = DataAccessLayer.Instance.InventoryUpdate(ReadProperty(_itemIdProperty), ReadProperty(_qtyProperty))
        End Using
        
	End Sub
	
	<Transactional(TransactionalTypes.TransactionScope)> _
	Protected Overrides Sub DataPortal_DeleteSelf()
        DataPortal_Delete(new InventoryCriteria(ItemId))
    End Sub
	
	<Transactional(TransactionalTypes.TransactionScope)> _
	Protected Overrides Sub DataPortal_Delete(ByVal criteria As Object)
		Dim theCriteria As InventoryCriteria = DirectCast(criteria, InventoryCriteria)
        If Not theCriteria Is Nothing Then
			Using reader As SafeDataReader = DataAccessLayer.Instance.InventoryDelete(theCriteria.StateBag)
			End Using
		End If
    End Sub

    #End Region
End Class